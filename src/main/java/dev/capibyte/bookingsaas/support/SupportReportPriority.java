package dev.capibyte.bookingsaas.support;

/** Triage priority for the super-admin support inbox — independent axis from
 * {@link SupportReportType} (what kind of submission) and from resolved status. */
public enum SupportReportPriority {
	BAJA,
	MEDIA,
	ALTA
}
